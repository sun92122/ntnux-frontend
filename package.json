{
  "name": "nuxt-app",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "nuxt build",
    "dev": "nuxt dev",
    "generate": "nuxt generate",
    "preview": "nuxt preview",
    "postinstall": "nuxt prepare",
    "deploy": "nuxt generate && echo ntnux.sun92122.com > .output/public/CNAME && echo > .output/public/.nojekyll && gh-pages -d .output/public --dotfiles"
  },
  "dependencies": {
    "@ag-grid-community/locale": "^32.0.0",
    "ag-grid-community": "^32.0.0",
    "ag-grid-vue3": "^32.0.0",
    "nuxt": "^3.12.3",
    "vue": "latest"
  },
  "devDependencies": {
    "gh-pages": "^6.3.0"
  }
}
